using OpenQA.Selenium;
using OpenQA.Selenium.Chrome;

namespace ConnextAutomation
{
    /*
    public class TestsDemo
    {
        private IWebDriver driver;

        //[SetUp]
        public void Setup()
        {
            driver = new ChromeDriver();
            driver.Navigate().GoToUrl("https://qa-odoo.connext.biz/");
            driver.Manage().Window.Maximize();
        }

        //[Test]
        public void Test1()
        {
            IWebElement userNameInput = driver.FindElement(By.Id("login"));
            userNameInput.SendKeys("dinh.ha@conceptia.com");

            IWebElement passwordInput = driver.FindElement(By.Id("password"));
            passwordInput.SendKeys("connext");

            IWebElement loginBtn = driver.FindElement(By.XPath(@"//button[@type='submit']"));
            loginBtn.Click();

            Thread.Sleep(5000);
            driver.Quit();
            Assert.Pass();
        }
    }*/
}